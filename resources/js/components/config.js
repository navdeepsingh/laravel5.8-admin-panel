const config = {};

config.fbshare = process.env.MIX_FBSHARE;
config.tweet = process.env.MIX_TWEET;
config.mail = process.env.MIX_MAIL;
config.whatsapp = process.env.MIX_WHATSAPP;

export default config;